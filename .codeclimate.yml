engines:
 duplication:
   enabled: true
   config:
     languages:
     - php
 phpcodesniffer:
   enabled: true
   config:
    standard: "ruleset.xml"

ratings:
 paths:
 - "**.php"

exclude_paths:
- "**/messages/"
