modules:
  config:
    WebDriver:
      browser: chrome
      port: 9515
      capabilities:
        chromeOptions:
          args: ['--headless', '--disable-gpu', '--no-sandbox',  '--window-size=1024,768']