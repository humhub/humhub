[phases.setup]
nixPkgs = [
  "php82",
  "php82Packages.composer",
  "nodejs_22"
]

[phases.build]
cmds = [
  "composer install --ignore-platform-reqs",
  "npm ci"
]

[start]
cmd = "chmod -R 777 /app/assets /app/uploads /app/protected/runtime /app/protected/config && php-fpm"
